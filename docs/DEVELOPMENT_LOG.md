# Development Log

A running record of every change to this codebase: what was done, why it was
done that way, and what alternatives were rejected. Newest entries at the
bottom.

**How to use this file.** Every change — a feature, a refactor, a deletion, a
renamed concept, a reversed decision — gets an entry. An entry that only says
*what* changed is incomplete; the value of this file is the *why*, because
that is the part that is invisible in a diff and lost within a month.

**Entry format**

```
## NNN — Title
Date · Area · Type (feature | refactor | fix | deletion | decision | doc)

**What.**   What changed, concretely.
**Why.**    The problem being solved.
**How.**    The approach, and the mechanism if it is non-obvious.
**Rejected.** What else was considered, and why it lost.
**Affects.** Files and concepts touched.
```

---

## 001 — Project shape: Django + DRF backend, React frontend
2026-09-02 · Whole project · decision

**What.** Backend is Django 5.1 + Django REST Framework. Frontend is a
separate React + Vite + TypeScript SPA using shadcn/ui. Two codebases, one
repository.

**Why.** The product is 130+ modules of business logic over a large
relational model — patients, stock ledgers, payroll, double-entry finance.
That is Django's home ground: the ORM, migrations, and admin remove months of
plumbing, and the transactional guarantees are what a stock ledger and a
payroll run actually need.

**How.** `backend/` (Django) and `frontend/` (React) are independent. The API
contract between them is OpenAPI, generated by drf-spectacular, so the
frontend's types can be generated rather than hand-written and drift.

**Rejected.**
- *Next.js* — was the initial choice and was reversed before any frontend
  code existed. Its server-rendering and routing strengths matter most for
  public, SEO-sensitive pages; this product is an authenticated back-office
  application where every screen is behind a login. The extra server runtime
  would be a second deployment target earning nothing.
- *Laravel* — faster CRUD scaffolding, but weaker for the real-time surfaces
  (ICU monitoring, queue displays, bed boards) and no shared type story.
- *Monolithic Django templates* — would not support the offline/degraded mode
  the spec requires for pharmacy POS and registration.

**Affects.** Repository layout, `backend/requirements.txt`, `frontend/`.

---

## 002 — Database per tenant
2026-09-02 · Tenancy · decision

**What.** Each customer organization gets its own PostgreSQL database. A
single *control-plane* database holds the SaaS business records.

**Why.** This system holds patient records. The strongest isolation
available, short of separate infrastructure, is a separate database: a query
that forgets its `WHERE organization_id = …` returns nothing rather than
another hospital's patients, because the rows are not there to return.

**How.** See `docs/adr/0001-database-per-tenant.md` for the full argument.
Mechanically: `apps/tenancy/` holds the tenant registry, a connection
registrar that adds databases to Django at runtime, a router that sends every
query to the right one, and middleware that binds the context per request.

**Rejected.**
- *Shared schema with `tenant_id` + row-level security* — cheapest to run and
  the industry default, but isolation rests on every query being correct
  forever, and one missing predicate is a cross-hospital data breach.
- *Schema-per-tenant (`django-tenants`)* — a reasonable middle ground, but it
  was not what was asked for, and per-database gives per-customer backup,
  restore and residency for free.

**Cost accepted.** No cross-database transactions, no cross-database foreign
keys, and migrations must fan out across every tenant. Entries 007 and 011
deal with the consequences.

**Affects.** `apps/tenancy/*`, `config/settings/base.py`.

---

## 003 — Two classes of app: control-plane and tenant
2026-09-02 · Architecture · decision

**What.** `CONTROL_PLANE_APPS` and `TENANT_APPS` in settings. Every app
belongs to exactly one list.

**Why.** The router needs to know, for any model, which database it lives in.
Deriving that per-model would scatter the decision; declaring it once per app
makes it reviewable in a single screen.

**How.** `TenantDatabaseRouter` reads both lists at construction and uses
them for `db_for_read`, `db_for_write` and `allow_migrate`. Because
`allow_migrate` is honoured, `migrate` against the control plane creates only
`cp_*` tables, and `migrate --database=tenant_x` creates only tenant tables.
Verified: the control-plane database has no `facility`, `role` or
`audit_event` table.

**Rejected.** A per-model `class Meta: database = …` attribute — Django has
no such hook, and a custom one would be invisible to `makemigrations`.

**Affects.** `config/settings/base.py`, `apps/tenancy/router.py`.

---

## 004 — A tenant model with no active tenant raises, never falls back
2026-09-02 · Tenancy · decision

**What.** `TenantDatabaseRouter` raises `TenantRoutingError` when a tenant
model is queried with no organization bound, instead of defaulting to the
control-plane database.

**Why.** A silent fallback is the worst possible failure here: a background
job that forgets to set context would write one customer's facility into the
shared database, and nothing would complain. Loud failure in development is
cheaper than silent corruption in production.

**How.** `_tenant_alias()` checks the context variable and raises with a
message that names the model and says how to fix it.

**Affects.** `apps/tenancy/router.py`, `apps/tenancy/context.py`.

---

## 005 — Tenant context uses `contextvars`, not thread locals
2026-09-02 · Tenancy · decision

**What.** The active organization is held in a `ContextVar`.

**Why.** Thread locals leak across `async` boundaries. Under ASGI, one thread
serves many coroutines; a thread-local tenant would let request A's
organization be visible inside request B. `ContextVar` is per-coroutine and
is inherited correctly by tasks.

**How.** `apps/tenancy/context.py` exposes `tenant_context()` as a context
manager so the previous value is always restored, including on exception.

**Affects.** `apps/tenancy/context.py`, all middleware.

---

## 006 — Identity lives in the control plane; clinical data does not
2026-09-02 · Identity · decision

**What.** `User`, `Membership`, `LoginAttempt` and `UserDevice` are
control-plane models. Roles and role assignments are tenant models.

**Why.** Login has to resolve *before* a tenant connection can be chosen — you
cannot look up a user in their organization's database when finding the
organization requires knowing the user. Beyond that bootstrap problem, one
person may work for two organizations in a group, and SSO/MFA are
platform-level concerns.

**How.** `RoleAssignment.user_id` is a bare `UUIDField`, not a foreign key,
because it points across a database boundary. `user_email` is denormalised
alongside it so listing assignments needs no cross-database lookup.

**Trade-off accepted.** Email addresses and names sit in a shared database.
That is a real reduction in isolation, and it is bounded: no patient,
clinical, financial or operational record leaves the tenant database.

**Rejected.** Users in tenant databases with a central email→organization
routing index — the index would hold the same emails anyway, so it buys
almost nothing while making every login two hops.

**Affects.** `apps/identity/models.py`, `apps/rbac/models.py`,
`docs/adr/0002-identity-placement.md`.

---

## 007 — Facility registry: a control-plane mirror of tenant facilities
2026-09-02 · Tenancy · feature

**What.** `FacilityRegistryEntry` duplicates non-clinical facility metadata
(code, name, type, status, district) into the control plane.

**Why.** Two things need facility data without a tenant connection:
1. **Quota checks.** "Does this organization already have three pharmacies?"
   must be answerable *inside the transaction* that approves a fourth.
   Opening a tenant connection mid-transaction to find out is both slow and
   impossible to make atomic.
2. **Platform analytics.** Counting hospitals across every customer would
   otherwise mean opening every tenant database in turn.

**How.** Written only by `FacilityService`, which updates both sides in one
logical operation. Holds no patient, clinical or financial data.

**Rejected.** Querying tenant databases on demand — correct but unusably slow
at the platform level, and unable to participate in the approving
transaction.

**Consequence.** Two databases cannot share a transaction, so drift is
possible. `reconcile_registry()` and the
`reconcile_facility_registry` management command detect and report it. Write
order is chosen so the surviving state after a partial failure is the safe
one: a registry row without a facility over-counts quota (visible, fixable),
whereas a facility without a registry row is invisible capacity.

**Affects.** `apps/tenancy/models.py`, `apps/organization/services.py`.

---

## 008 — Entitlements are layered and explain themselves
2026-09-02 · Entitlements · feature

**What.** An entitlement resolver that computes, for one organization, which
modules and features are on and what every numeric limit is — with the
provenance of each answer.

**Why.** The instruction was explicit: never gate on `plan == "premium"`.
Plans get renamed, discounted, merged and grandfathered; capabilities are
what the product actually depends on.

**How.** Four layers, weakest first:

| Layer | Source | Combines by |
|---|---|---|
| 1 | Plan | base value |
| 2 | Add-ons (× quantity) | addition |
| 3 | Grants (temporary) | addition |
| 4 | Overrides (contract) | replacement |

Overrides *replace* rather than add, because a negotiated contract term is a
statement about the final number, not an adjustment to it. Every resolved key
carries a `sources` list, so support can answer "why can this customer only
open two pharmacies?" by reading it rather than guessing.

**Rejected.** A single `limits` JSON blob on the subscription — no
provenance, no expiry, and every negotiated exception becomes a manual edit
with no record of who made it or why.

**Affects.** `apps/catalog/`, `apps/entitlements/`.

---

## 009 — Unknown limit keys resolve to zero, not unlimited
2026-09-02 · Entitlements · decision

**What.** `ResolvedEntitlements.limit()` returns a spec with `value = 0` for
a key nobody has defined.

**Why.** Failing closed. A typo in a limit key should stop an action, not
silently grant infinite headroom. The failure mode of "blocked, and someone
asks why" is recoverable; "unlimited, and nobody notices for a year" is not.

**Affects.** `apps/entitlements/resolver.py`.

---

## 010 — Per-facility-type limits derive from the overall ceiling
2026-09-02 · Entitlements · feature

**What.** A plan that says `max_facilities = 5` and nothing per type gives
every type a ceiling of 5 — but only for types whose module the customer has.
The overall ceiling still applies on top.

**Why.** Requiring every plan to enumerate all eight facility types would be
noise, and an unstated type defaulting to zero would block a perfectly
legitimate clinic. Meanwhile, a customer with 5 facilities must not be able
to open 5 clinics *and* 5 pharmacies — hence checking both.

**How.** `_derive_facility_type_limits()` fills gaps after the four layers
resolve. A type whose module is not entitled derives to 0 with the reason
recorded in `sources`.

**Affects.** `apps/entitlements/resolver.py`, `apps/catalog/keys.py`.

---

## 011 — Enforcement is a property of the limit, not a global rule
2026-09-02 · Entitlements · decision

**What.** Every limit carries an enforcement mode: `hard` (block), `soft`
(allow and flag), `grace` (allow until a date), `metered` (allow and bill).

**Why.** Not every ceiling should be a wall. Refusing to save a scan because
a hospital is 2 GB over its storage allowance is a decision a healthcare
system should not make on its own. Refusing a fourth pharmacy branch is
exactly right — that is a deliberate, reversible commercial act.

**How.** Storage is seeded as `metered` with an overage price; facility and
user counts are `hard`.

**Affects.** `apps/catalog/models.py`, `apps/entitlements/services.py`,
`seed_catalog`.

---

## 012 — Quota checks return a decision object, not a boolean
2026-09-02 · Entitlements · decision

**What.** `check_quota()` returns a `QuotaDecision` carrying the limit,
current usage, remaining headroom, enforcement mode, provenance and a list of
remediation options. It does not raise; callers opt in via
`.raise_if_blocked()`.

**Why.** A boolean can only produce a wall. Returning the full position lets
the UI say "you are using 2 of 3 pharmacies" *before* the user fills in a
form, and lets a blocked action explain itself and offer a route forward —
buy an add-on, close a facility, upgrade, or ask for an exception.

**Affects.** `apps/entitlements/services.py`,
`GET /api/org/facilities/capacity/`.

---

## 013 — Facility changes go through request-and-approval, not CRUD
2026-09-02 · Provisioning · feature

**What.** Opening, closing, re-opening, suspending or converting a facility
is a `FacilityChangeRequest` that is evaluated, routed to an approver, and
executed only on approval. There is no facility create/update/delete
endpoint.

**Why.** Adding or removing a hospital, clinic or pharmacy changes what the
customer pays, what their staff can do, and what the platform must support.
As a plain "New Facility" button it also becomes a way to churn capacity —
open a branch for a week, close it, open another — which makes both billing
and capacity planning fiction. Routing it through approval produces a record
of not just what the estate looks like, but why.

**How.**
1. **Evaluate** — quota decisions, module entitlement and churn history are
   computed and frozen onto the request, so a reviewer sees the position as
   it stood when the request was raised.
2. **Route** — the approval level is *derived*, never chosen by the
   requester. Inside entitlement and non-destructive → the customer governs
   it. Beyond entitlement, or churn-flagged → the platform does.
3. **Decide** — approval at the final required level executes immediately;
   there is no separate "apply" step to forget.
4. **Execute** — re-checks quota, then writes the facility and the registry
   entry.

**Rejected.** A permission (`facility.create`) and an ordinary endpoint —
enforces *who* may act but records nothing about *why*, and cannot express
"allowed, but somebody senior should look at this".

**Affects.** `apps/provisioning/`, `apps/organization/services.py`,
`apps/organization/views.py`, `apps/platform_api/views.py`.

---

## 014 — Approval is permission to act, not a licence to act regardless
2026-09-02 · Provisioning · decision

**What.** `execute_request()` re-runs the quota check immediately before
applying an approved change.

**Why.** Time passes between approval and execution — a scheduled request may
sit for weeks — and the customer's entitlement may have moved: a grant
expired, an add-on lapsed, another branch opened. Executing on a stale
approval would put the customer over their entitlement with an audit trail
that says it was fine.

**How.** On failure the request moves to `failed` with the blocking decisions
recorded, rather than silently proceeding.

**Affects.** `apps/provisioning/services.py`.

---

## 015 — Churn detection surfaces a pattern; it does not judge it
2026-09-02 · Provisioning · feature

**What.** Opening a facility of a type the organization has closed twice in
the last 90 days escalates the request to platform review.

**Why.** Closing a branch and opening another is often legitimate — a
relocation, a lease ending. It is also the obvious way to run more sites than
you pay for. Blocking it outright would punish the honest case; ignoring it
makes the limit advisory.

**How.** `detect_churn()` returns the closure history with dates and hands it
to a human, who can see a relocation for what it is. Thresholds are per
organization via `ChangeRequestPolicy`.

**Rejected.** A hard cooldown blocking re-opening for N days — punishes
relocations, and is trivially worked around by waiting.

**Affects.** `apps/provisioning/services.py`, `apps/provisioning/models.py`.

---

## 016 — Closing frees quota; suspending does not
2026-09-02 · Entitlements · decision

**What.** `FacilityRegistryEntry.counts_towards_quota` is True for `pending`,
`active` and `suspended`; only `closed` releases the slot.

**Why.** Suspension is a state the organization can reverse at will. If it
freed the slot, a customer could hold twice their entitlement by rotating
suspensions. Closing is a deliberate act that goes through approval and is
recorded, which is what makes it safe to honour.

**Affects.** `apps/tenancy/models.py`, `apps/entitlements/services.py`.

---

## 017 — Segregation of duties, declared and enforced
2026-09-02 · RBAC · feature

**What.** Permissions declare `conflicts_with` pairs
(`purchase.create` ⁄ `purchase.approve`, `payroll.process` ⁄
`payroll.approve`, `facility.request_change` ⁄ `facility.approve_change`, …).
Conflicts are reported when a role is saved, and the runtime refuses to let
one person both raise and approve the same record.

**Why.** Maker-checker is only real if it is enforced at the moment of
approval. Catching it when a role is *saved* is the earlier, cheaper warning;
catching it at approval is the one that actually holds.

**How.** `conflicting_permissions()` for the design-time check;
`_assert_may_decide()` and `assert_different_actors()` for the runtime one.
Toggleable per organization, because a two-person clinic cannot segregate
anything and would otherwise be unable to operate.

**Affects.** `apps/rbac/permissions.py`, `apps/rbac/services.py`,
`apps/provisioning/services.py`.

---

## 018 — Permissions are code; roles are data
2026-09-02 · RBAC · decision

**What.** The permission catalogue is a Python tuple. Roles are database rows
customers can create freely.

**Why.** A permission is a promise the application makes about what it
checks. Letting an administrator type a new permission would create one that
nothing enforces — a checkbox that does nothing, which is worse than no
checkbox. Roles carry no such promise: they are just bundles.

**How.** `Role.permissions` is a JSON list of codes, validated against
`PERMISSION_CODES` at resolution time, with unknown codes logged and ignored.

**Affects.** `apps/rbac/permissions.py`, `apps/rbac/models.py`.

---

## 019 — Scope filters queries; it does not just refuse them
2026-09-02 · RBAC · decision

**What.** Permissions are granted at a scope (own → own patients → unit →
department → facility → multiple facilities → organization).
`accessible_facility_ids()` turns a grant into a queryset filter.

**Why.** A department-scoped user asking for the facility list should get
*their* facility, not a 403. Refusing would be technically correct and
practically useless.

**How.** `FacilityViewSet.get_queryset()` narrows by the caller's scope.
`None` means unrestricted; an empty set means nothing is visible.

**Affects.** `apps/rbac/services.py`, `apps/common/permissions.py`,
`apps/organization/views.py`.

---

## 020 — Denials beat grants
2026-09-02 · RBAC · decision

**What.** A `PermissionOverride` with `is_granted = False` removes a
permission even when a role grants it.

**Why.** The entire point of withholding `refund.approve` from one manager is
that their role would otherwise carry it. A denial that could be out-voted
would be decorative.

**Note.** Organization owners bypass permission checks entirely, so a denial
against an owner has no effect. This is logged when it happens rather than
failing silently.

**Affects.** `apps/rbac/services.py`.

---

## 021 — The audit log lives in the tenant database
2026-09-02 · Audit · decision

**What.** `AuditEvent` and `EntityVersion` are tenant models.

**Why.** The audit log records *who read which patient's file*, which is
itself patient-identifiable. Centralising it would recreate, in one shared
table, exactly the exposure that database-per-tenant exists to prevent.

**How.** Append-only by database grant (INSERT + SELECT for the application
role), not by an ORM guard — a guard in application code is theatre, since
anything that can bypass the ORM bypasses the guard.

**Affects.** `apps/audit/`, `docs/security/audit-immutability.md`.

---

## 022 — Audit writes never raise
2026-09-02 · Audit · decision

**What.** `record()` catches every exception and logs it.

**Why.** A nurse recording vitals must not be blocked because the audit
table is unavailable. The clinical action is the thing that matters; a
missing log line is a platform incident, not a patient-facing failure.

**How.** Failures are logged at `exception` level with the action and actor,
so they are loud in monitoring even though they are silent to the user.

**Affects.** `apps/audit/services.py`.

---

## 023 — Audit diffs store changed fields only
2026-09-02 · Audit · decision

**What.** `diff_instance()` records only fields that changed, with a redaction
list for secrets.

**Why.** Storing full before/after copies would duplicate the medical record
into the audit log — multiplying the data at risk for no investigative gain.
Records where full history genuinely matters (prescriptions, invoices,
clinical notes, compensation) use `EntityVersion` snapshots instead.

**Affects.** `apps/audit/services.py`.

---

## 024 — One error envelope for the whole API
2026-09-02 · API · decision

**What.** Every error returns `{"error": {"code", "message", "detail"}}`.

**Why.** The frontend renders one error component and branches on `code`
rather than parsing prose. It also means a new error type cannot accidentally
ship in a shape nothing handles.

**How.** `api_exception_handler` rewraps DRF, Django validation, 404 and
permission errors into the same shape. `DomainError` subclasses carry their
own code and status.

**Affects.** `apps/common/exceptions.py`, `config/settings/base.py`.

---

## 025 — `/api/auth/` is not wholly exempt from tenant resolution
2026-09-02 · Tenancy · fix

**What.** The middleware's skip list originally contained the prefix
`/api/auth/`. Narrowed to the specific pre-authentication endpoints
(`login`, `refresh`, `register`, `password`).

**Why.** Bug found while wiring URLs: `/api/auth/session/` returns the
caller's resolved permissions, which live in the *tenant* database. With the
whole prefix skipped, no tenant was ever bound and the endpoint could not
have worked.

**Affects.** `apps/tenancy/middleware.py`.

---

## 026 — Three dataclass fields were silently not fields
2026-09-02 · Entitlements, Audit · fix

**What.** `ResolvedEntitlements.resolved_at`, `LimitSpec.overage_unit_price`
and four `AuditContext` attributes were written as `name = None` without a
type annotation, making them class attributes rather than dataclass fields.
Annotated as `object = None`.

**Why.** Found at runtime: `ResolvedEntitlements(resolved_at=…)` raised
`TypeError: unexpected keyword argument`. `AuditContext` would have failed
the same way on the first authenticated request — the audit middleware passes
all four by keyword.

**Lesson.** `@dataclass` silently ignores un-annotated assignments. Worth a
lint rule.

**Affects.** `apps/entitlements/resolver.py`, `apps/audit/services.py`.

---

## 027 — Added the `operations_manager` role
2026-09-02 · RBAC · feature

**What.** A new system role holding `facility.request_change` at organization
scope.

**Why.** Found by the demo seed failing. `facility_manager` caps at facility
scope — correctly, it is a facility role — but that made it impossible for
anyone except an organization owner to raise the request for the *first*
facility, since no facility exists to be scoped to. The owner cannot raise it
either, because segregation of duties stops them approving their own request.

The gap was in the role set, not the engine: estate planning is an
organization-wide job and deserves its own role. The `max_scope` guard that
surfaced it was working exactly as intended.

**Affects.** `apps/rbac/services.py`, `seed_demo`.

---

## 028 — The demo seed uses the real code paths
2026-09-02 · Tooling · decision

**What.** `seed_demo` creates facilities by raising change requests and
approving them, not by calling `Facility.objects.create()`.

**Why.** A seed script that bypassed the workflow would be seeding a state
the application itself cannot produce — and would stop being a test of
anything. As written, running the seed exercises entitlement resolution,
quota checks, routing, approval, segregation of duties, cross-database
writes and registry mirroring in one command.

**Result on first run.** Three facilities opened via organization approval; a
hospital request correctly escalated to platform review, because the
Professional plan does not include the hospital module.

**Affects.** `apps/tenancy/management/commands/seed_demo.py`.

---

## 029 — This development log
2026-09-02 · Documentation · doc

**What.** This file, plus a commitment to inline commentary in the backend
explaining *why* a piece of code exists, including what each import is for
and where it is used.

**Why.** Requested directly, and correct: this codebase encodes a large
number of judgement calls (why suspension does not free quota, why approval
re-checks entitlement, why denials beat grants) that are invisible in the
code itself. Six months on, an undocumented judgement call is
indistinguishable from an accident, and gets "cleaned up".

**Affects.** `docs/DEVELOPMENT_LOG.md`, all backend modules.

---

## 030 — Tenant middleware authenticates the JWT itself
2026-09-02 · Tenancy · fix

**What.** `TenantContextMiddleware` now calls
`JWTAuthentication().authenticate(request)` when `request.user` is anonymous,
and publishes the result onto the request.

**Why.** Found by smoke-testing the API: `/api/auth/session/` returned
`organization: null` and `/api/org/facilities/capacity/` returned 403 for a
user who demonstrably had both. The cause is an ordering problem that is easy
to miss and total in effect:

- Django's `AuthenticationMiddleware` populates `request.user` from the
  **session**. This is a token-authenticated API, so it always yielded
  `AnonymousUser`.
- DRF authenticates the bearer token **inside the view**, via
  `APIView.initial()`.
- The tenant middleware runs between those two, so it never saw an
  authenticated user and never bound an organization.
- The database router therefore had no tenant for every single request.

Nothing in the stack complained; the system simply behaved as though every
caller were logged out.

**How.** The middleware verifies the token itself. The cost is one extra
signature verification per request — DRF still authenticates independently in
the view. That is cheap, and the alternative designs are worse:

- *Resolve the tenant lazily on first use* — the router needs the context
  variable set, and a lazy value cannot set a `ContextVar` retroactively for
  code already running.
- *Bind the tenant in a DRF base view* — works, but only for views that
  remember to inherit it. Middleware cannot be forgotten.

A bad token is deliberately **not** rejected in middleware: that is DRF's
job, and doing it here would bypass the API error envelope and return a bare
Django 401. The request proceeds with no tenant, and the view's own
authentication produces the correct, well-formed error.

**Affects.** `apps/tenancy/middleware.py`.

---

## 031 — End-to-end verification of the entitlement path
2026-09-02 · Entitlements · doc

**What.** The full path was exercised against a running server, not just unit
logic.

**Result.**

| Check | Outcome |
|---|---|
| `GET /api/auth/session/` | organization bound, 10 permissions resolved, plan `professional` |
| `GET /api/org/facilities/` | 3 facilities, departments auto-seeded per type |
| `GET /api/org/facilities/capacity/` | overall 3/5; pharmacy 1/3; laboratory 1/2 |
| Preview a 4th pharmacy | `within_entitlement: true`, routed to organization approval |
| Preview a hospital | `within_entitlement: false`, routed to **both** — refused with two stacked reasons: the `hospital` module is not in the plan, *and* the derived per-type limit is 0 |

The hospital case is the one worth noting: the per-type limit derived to 0
*because* the module is absent (entry 010), so the customer gets a precise
explanation rather than a generic "upgrade your plan".

**Affects.** No code; recorded as evidence the design holds.

---

## 032 — Frontend scaffold: React + Vite + TypeScript
2026-09-02 · Frontend · feature

**What.** `frontend/` scaffolded with Vite's `react-ts` template, plus
Tailwind and shadcn/ui component primitives.

**Why.** shadcn/ui is copy-in source rather than an installed dependency, so
components can be edited directly — which matters for a product that will
need dense, domain-specific tables and forms that no generic component
library gets right (batch selection with expiry dates, bed boards, triage
queues).

**How.** The API client attaches the bearer token and the `X-Organization`
header on every request. The organization header is what makes the context
switcher work: the same screens re-render against a different tenant purely
by changing that value.

**Affects.** `frontend/`.

---

## 033 — Facility-type limit add-ons were unsellable without module add-ons
2026-09-02 · Catalogue · fix

**What.** Added `module_hospital`, `module_clinic`, `module_pharmacy`,
`module_laboratory` and `module_warehouse` add-ons.

**Why.** Found while approving a real request. `check_facility_quota` asks
three questions, and the first is "is the module entitled at all?" (entry 013).
The seeded catalogue had `extra_hospital`, which raises the *count* — but no
way to buy the hospital *module*. So a platform approver faced with a customer
who wanted a hospital had nothing to sell them: the capacity existed in the
catalogue but was unreachable.

**Lesson.** A per-type limit add-on is meaningless without the module add-on
for that type. Any new facility-bearing module needs both, or its capacity
cannot be sold.

**Affects.** `seed_catalog`.

---

## 034 — An approver can attach several add-ons in one decision
2026-09-02 · Provisioning · feature

**What.** `decide()` and the decision endpoints accept `granted_addons`, a
list of `{code, quantity}`. The single-add-on arguments remain for the common
case.

**Why.** Directly out of entry 033: making a hospital request fit needs *two*
purchases — the module and the slot. Forcing two separate decisions would put
the request into an incoherent state between them, where the module is bought
but the count still says zero.

**How.** `_normalise_addon_grants()` collapses both argument forms into one
list and sums duplicate codes, so a caller passing the same add-on twice does
not silently double the customer's bill.

**Affects.** `apps/provisioning/services.py`,
`apps/organization/serializers.py`, `apps/platform_api/views.py`.

---

## 035 — Soft delete and unique constraints were fighting each other
2026-09-02 · Data model · fix

**What.** Every unique constraint on a soft-deletable model is now partial:
`condition=Q(deleted_at__isnull=True)`.

**Why.** Found by re-running `seed_catalog`, which failed with
`duplicate key value violates unique constraint "uniq_module_per_plan"`.

The cause is general, not a seed problem. `SoftDeleteQuerySet.delete()` sets
`deleted_at` rather than removing the row — so the row is still in the table,
still occupying the unique key, still blocking anything that wants to reuse
that value. Every soft-deletable model with a unique constraint had this:

- a customer closing facility `MKC-KTM` could never reuse that code;
- a revoked membership blocked re-inviting the same person;
- re-seeding a plan's modules was impossible.

Each would have surfaced as an integrity error saying nothing useful about
what actually went wrong.

**How.** Partial constraints on `PlanModule`, `PlanFeature`, `PlanLimit`,
`FacilityRegistryEntry`, `Department`, `Unit`, `ConfigSetting`, `Membership`
and `UserDevice`. Uniqueness now holds among *live* rows, which is what was
meant all along.

**Also.** The seed uses `hard_delete()` for plan join rows. Those are
structural — a plan's module list is a definition, not an event — so
tombstoning them serves nobody.

**Lesson.** Adding `SoftDeleteModel` to a model with a unique constraint is
never a drop-in change. Worth a check in review.

**Affects.** `apps/catalog/models.py`, `apps/tenancy/models.py`,
`apps/organization/models.py`, `apps/identity/models.py`, `seed_catalog`,
four migrations.

---

## 036 — A failed execution rolls back its own approval
2026-09-02 · Provisioning · doc

**What.** Documenting behaviour discovered by testing, not a change.

Approving a request whose capacity no longer fits raises inside
`execute_request()`. Because `decide()` is `@transaction.atomic`, that
exception rolls back *the whole decision* — the `ChangeRequestDecision` row,
the status change, everything. The request is left exactly as it was, open and
awaiting a decision.

**Why this is right.** An approval that authorised something impossible was
never a valid approval. Recording it would leave a request marked "approved"
that had not happened, and a decision log claiming someone signed off on
something that could not be done. Rolling back leaves the approver able to
retry with the capacity attached — which is the actual fix.

**Note for future refactoring.** This behaviour depends on `execute_request()`
being called *inside* `decide()`'s transaction. Moving execution to a
background task or a `transaction.on_commit` hook would break it: the approval
would then commit and the execution would fail separately, producing exactly
the incoherent state described above. If execution is ever made asynchronous,
the request must move to an explicit `failed` state that can be retried.

**Verified.** Approving `FCR-2026-0004` without capacity was refused; the
request stayed at `platform_review` with only the earlier organization
approval recorded. Approving again with the module and the slot executed
cleanly.

**Affects.** No code. `apps/provisioning/services.py` behaviour.

---

## 037 — Decision responses re-fetch before serializing
2026-09-02 · API · fix

**What.** Both `decide` endpoints re-query the request before serializing the
response.

**Why.** The object came from a queryset with `prefetch_related("decisions")`,
and that cache was populated *before* the new decision existed. The response
therefore omitted the very decision the caller had just made — a UI rendering
it would show the approval as not having happened.

Small bug, and a misleading one: it looks exactly like the write failing.

**Affects.** `apps/platform_api/views.py`, `apps/organization/views.py`.

---

## 038 — Documentation set
2026-09-02 · Documentation · doc

**What.** `README.md`, `docs/adr/0001-database-per-tenant.md`,
`docs/adr/0002-identity-placement.md`.

**Why.** Two decisions shape everything else and both look strange without
their reasoning: database-per-tenant (which costs cross-database transactions)
and identity-in-the-control-plane (which appears to contradict it). ADR 0002
states the boundary precisely — names and emails are shared, clinical and
financial data never is — so a future change that crosses it has to argue
against something explicit rather than drift past a gap.

**Affects.** `README.md`, `docs/adr/`.

---

## 039 — Implementation checklist
2026-09-02 · Documentation · doc

**What.** `docs/IMPLEMENTATION_CHECKLIST.md` — all 132 specification sections
with status, grouped into eleven delivery phases.

**Why.** The development log records *why* each decision was made; it does not
answer "how much is left?". With a specification this large, that question is
asked constantly, and answering it from memory produces different answers each
time.

**Also.** The checklist carries a set of cross-cutting invariants — tenant
context, entitlement checks, audit, segregation of duties, scope filtering,
versioning, error envelope, log entry. Those are the rules every new module
must hold to, and having them in one list makes a review checkable.

**Affects.** `docs/IMPLEMENTATION_CHECKLIST.md`.

---

## 040 — Patient master
2026-09-02 · Clinical · feature

**What.** `apps.patients`: `Patient`, `PatientIdentifier`, `PatientAllergy`,
`PatientCondition`, `PatientMergeLog`. A tenant app.

**Why.** Everything clinical, most of billing and all of diagnostics acquires
a foreign key to the patient, which makes it the hardest model in the system
to change later. Three properties are therefore built in rather than
retrofitted:

1. **One patient, many facilities.** Registration is per *organization*. A
   patient registered at the Kathmandu clinic and later admitted to the
   Bhaktapur hospital is one person with one history.
2. **Identity in Nepal is messy.** Many patients have no national ID; names
   transliterate inconsistently; dates of birth are often approximate. Hence
   `is_dob_estimated`, `stated_age_years`, and identifiers as a *list of
   claims* rather than a column.
3. **Duplicates are inevitable.** A 2 a.m. walk-in will be registered again.
   The design detects and merges rather than pretending to prevent.

**Specific decisions.**

- *Name stored in parts.* Nepali names do not split reliably on whitespace —
  "Krishna Bahadur Shrestha" has a middle name, "Sita Devi" does not.
- *`age_years` returns `None`, not 0, when unknown.* A newborn and an unknown
  age must not look identical to a dosing calculation.
- *MRN allocated under `select_for_update`.* Counting rows would reuse a
  number after a delete, and two concurrent registrations at a busy counter
  would collide.
- *`merged_into` rather than deletion.* Prescriptions and invoices already
  point at the duplicate; a merge must never orphan a clinical record.
  `resolve()` follows the chain, with cycle protection.

**Affects.** `apps/patients/`.

---

## 041 — Duplicate detection scores, it does not decide
2026-09-02 · Clinical · feature

**What.** `find_duplicate_candidates()` returns weighted candidates. A shared
identifier scores 60, a phone 25, name-plus-date-of-birth 20, name alone 10.
Registration raises HTTP 409 with the candidates rather than an error.

**Why.** Both failure modes are expensive and they pull in opposite
directions. A missed duplicate splits one patient's history across two records
and is painful to unpick; a false positive that *blocks* registration stops a
real patient being seen. So the system surfaces candidates and lets a human
decide, with `force=true` to proceed.

**Why 409 rather than 400.** It is not a validation failure. Nothing the clerk
typed is wrong — the server is asking them to look before creating a second
record. The status distinguishes "you made a mistake" from "please confirm".

**Affects.** `apps/patients/services.py`, `apps/patients/views.py`,
`frontend/src/pages/Patients.tsx`.

---

## 042 — Reads of patient records are audited, not only writes
2026-09-02 · Audit · decision

**What.** `PatientViewSet.retrieve` calls `record_patient_access()`, writing a
`VIEW_SENSITIVE` audit event.

**Why.** "Who looked at this record?" is the question asked after a privacy
complaint, and it cannot be answered retrospectively. Logging only changes
would leave the most common form of privacy breach — an unauthorised *look* —
entirely invisible.

**Affects.** `apps/patients/views.py`, `apps/patients/services.py`.

---

## 043 — Appointments and the queue live in one app
2026-09-02 · Scheduling · decision

**What.** `apps.scheduling` holds `ProviderSchedule`, `ScheduleException`,
`Appointment` and `QueueToken`.

**Why.** They are the same problem from two ends. An appointment is a claim on
a provider's future time; a queue token is a claim on their time *today*. In a
Nepali OPD both arrive at once — a booked follow-up and a walk-in who has
travelled four hours — and whatever sequences them has to see both. Splitting
them across apps would put a circular dependency between the two.

**Specific decisions.**

- *Sunday is weekday 0.* The Nepali working week runs Sunday to Friday.
- *`NO_SHOW` is distinct from `CANCELLED`.* A cancellation freed the slot; a
  no-show wasted it. Conflating them makes utilisation unmeasurable.
- *`walk_in_reserve`.* Online booking cannot consume the whole diary, so a
  patient who travelled in is not turned away by a full remote schedule.
- *Four timestamps on an appointment* (`arrived_at`,
  `consultation_started_at`, `consultation_ended_at`, plus `scheduled_for`).
  Each is the boundary of an interval a patient actually experiences. Without
  all four, section 21's waiting-time requirement cannot be answered.
- *`SKIPPED` rather than deleting a token.* A patient who missed their call
  because they stepped out is re-queued, and the fact that they were called
  stays on the record.
- *Emergencies get priority 100*, not merely a higher number — triage should
  not be a matter of degree when someone is critically unwell.

**Affects.** `apps/scheduling/`.

---

## 044 — `@transaction.atomic` was opening the wrong database
2026-09-02 · Tenancy · fix · **important**

**What.** Added `apps/tenancy/db.py` with `tenant_atomic()` and
`@tenant_atomic_method`. Replaced nine bare `@transaction.atomic` decorators
in the patient and scheduling services.

**Why.** `transaction.atomic()` with no arguments opens a transaction on the
**default** database. Under database-per-tenant that is the control plane —
which is almost never the database the code is writing to.

The failure is quiet and nasty. A decorated service that writes tenant rows
*appears* to work: the writes succeed, because Django autocommits them on the
tenant connection. What is silently lost is the guarantee. The block is not
atomic, a failure half-way leaves the tenant database with partial data, and
nothing complains.

It surfaced only because `Patient.allocate_mrn()` uses `select_for_update`,
which raised `TransactionManagementError: select_for_update cannot be used
outside of a transaction`. Without that row lock the bug would have shipped —
a service that looks transactional and is not.

`FacilityService` already got this right, using `atomic(using=alias)`
explicitly. The new services did not, which is exactly why it needed to be a
named helper rather than a convention people are expected to remember.

**How.** `tenant_atomic()` resolves the alias from the active context and
raises `NoTenantBound` if none is set — so the *other* failure mode, running
tenant work with no context, is loud rather than silent.

**Lesson.** Never write a bare `@transaction.atomic` in code touching tenant
models. Added to the checklist's cross-cutting invariants.

**Affects.** `apps/tenancy/db.py`, `apps/patients/services.py`,
`apps/scheduling/services.py`.

---

## 045 — Patient limits are soft; facility limits are hard
2026-09-02 · Entitlements · decision

**What.** `max_patients` added to every plan, enforced `SOFT`.

**Why.** Found because the fail-closed default (entry 009) blocked the
clinical seed: no plan defined a patient ceiling, so it resolved to 0 and
registration was refused. The guard worked exactly as designed and caught a
real gap.

The interesting part was choosing the enforcement. A **hard** patient limit
means refusing to register a sick person standing at a counter because their
clinic has outgrown its subscription — a commercial dispute resolved at the
patient's expense. `SOFT` allows the registration, flags the overage, and
leaves the account team a conversation to have the next morning.

This is entry 011's principle applied where it matters most. The current
split:

| Limit | Enforcement | Why |
|---|---|---|
| `max_patients` | soft | Somebody is waiting to be seen |
| `max_storage_gb` | metered | Refusing to save a scan is not our call |
| `max_facilities*` | hard | Deliberate, reversible, nobody waiting |
| `max_users` | hard | Same |

**Affects.** `seed_catalog`.

---

## 046 — "Free slots" meant the wrong thing
2026-09-02 · Scheduling · fix

**What.** `provider_day_view()` now returns `capacity`, `booked` and
`remaining_capacity` instead of a bare `free_slots` count.

**Why.** Caught by reading the seeded output: Dr Karki showed **9/9 free**
while holding four appointments. Not a bug in the counting — with
`slot_capacity = 2`, every slot genuinely still had room. But a receptionist
reads "9/9 free" as an empty diary and books into it.

The old field answered "how many slot times have space?"; the question
actually being asked is "how many more patients fit?". Now reads
`booked 4/18, remaining 14`.

**Lesson.** A correct number under a misleading name is a bug. Overbooking
capacity made the two diverge.

**Affects.** `apps/scheduling/services.py`,
`frontend/src/pages/Queue.tsx`.

---

## 047 — Clinical frontend
2026-09-02 · Frontend · feature

**What.** `Patients.tsx` (search, registration, duplicate confirmation,
record panel) and `Queue.tsx` (live queue, call/start/complete, today's
sessions).

**Specific decisions.**

- *Search is one box.* At a counter the clerk has whatever the patient can
  give them — a card, a phone number, half a name — and should not have to
  decide which field it belongs in first. Debounced at 300 ms.
- *Blocking allergies render first, in red.* A severe penicillin allergy
  below an address is a patient-safety problem, not a layout preference.
- *Duplicate candidates are a decision, not an error.* The panel offers "use
  an existing record" and "this is a different person" rather than a
  dismissible warning.
- *The queue polls at 15 s rather than using a WebSocket.* A queue changes
  every few minutes; a socket that must survive a Nepali clinic's
  connectivity is a lot of machinery for a number that can be seconds stale
  without harm.
- *Average wait colours at 30 and 45 minutes* — roughly where a waiting room
  starts to feel broken.

**Affects.** `frontend/src/pages/Patients.tsx`,
`frontend/src/pages/Queue.tsx`, `frontend/src/types/index.ts`,
`frontend/src/App.tsx`.

---

## 048 — Encounters
2026-09-02 · Clinical · feature

**What.** `apps.encounters`: `Encounter`, `VitalSigns`, `ClinicalNote`,
`Diagnosis`. A tenant app.

**Why.** An encounter is the unit clinical work attaches to. A patient is a
person; an encounter is one occasion on which that person was seen. Vitals,
notes, diagnoses, orders and charges hang off the encounter rather than the
patient, because "what was their blood pressure?" is meaningless without
"when, and who took it?".

**Shaped for inpatient before inpatient exists.** An OPD consultation and a
three-week admission are the same entity with a different `encounter_type` and
duration. Building a separate IPD record later would mean two parallel
clinical histories per patient, which is how systems end up unable to answer
simple questions.

**Specific decisions.**

- *Vitals stored as a set, not as individual observations.* A blood pressure
  without the pulse and temperature taken alongside it is much harder to
  interpret. Several sets per encounter is normal — on arrival, after
  treatment, before discharge.
- *Every vitals field nullable.* A nurse taking a temperature in triage should
  not have to invent a respiratory rate.
- *`on_room_air` on the oxygen saturation.* 94% on air and 94% on four litres
  are very different observations, and a number without that context is
  clinically misleading.
- *SOAP as four fields, not one blob.* The structure is what lets a discharge
  summary pull the assessment and an audit ask whether a plan was recorded.
  Neither is possible over free text.
- *`AWAITING_RESULTS` distinct from `IN_PROGRESS`.* One needs the doctor's
  attention now; the other is waiting on the laboratory. A worklist that
  conflates them is noise.
- *Triage as 1–5 integers.* Ordering a queue by the field puts the sickest
  first with no lookup table.
- *`Diagnosis` separate from `PatientCondition`.* A diagnosis is what was
  decided at one visit; a condition is what the patient carries between
  visits. Promotion from one to the other is deliberate (entry 052).

**Affects.** `apps/encounters/`.

---

## 049 — Signed notes are amended, never edited
2026-09-02 · Clinical · decision

**What.** Signing a note locks it and snapshots it to `EntityVersion`.
Corrections create an amendment row pointing at the original, which stays
readable and unchanged.

**Why.** A clinician cannot un-say something. A signed note is a clinical and
legal statement, and "what did the record say before it was changed?" is a
question asked in exactly the situations where the answer matters most.
Editing in place makes it unanswerable.

**How.** `amend_note()` requires a reason and refuses to amend an unsigned
note (that one is edited directly — the ceremony would be pointless).
`sign_note()` snapshots the content, so the version record proves what the
note said at the moment of signature.

**Affects.** `apps/encounters/models.py`, `apps/encounters/services.py`.

---

## 050 — Closing an encounter requires documentation
2026-09-02 · Clinical · decision

**What.** `close_encounter()` refuses when there is no note, no diagnosis and
no vitals.

**Why.** A visit with nothing recorded is almost always an encounter opened by
mistake — a mis-click on a queue, a duplicate. Closing it silently makes the
mistake permanent and puts a phantom visit in the patient's history. The error
message says to cancel it instead, which is the honest action.

**Affects.** `apps/encounters/services.py`.

---

## 051 — Prescribing: nothing is overwritten
2026-09-02 · Prescribing · feature

**What.** `apps.prescriptions`: `Prescription` and `PrescriptionLine`.
Changing a prescription creates a new version and supersedes the old one;
stopping a drug records who, when and why.

**Why.** A prescription is a clinical instruction and a legal record. "What
were they taking on the 14th?" must be answerable years later, and it cannot
be if edits mutate rows.

**Also: the medicine is denormalised onto the line.** Each line stores the
drug's name, strength and form as text *as well as* a `product_uuid` pointing
at the future pharmacy catalogue. Products get renamed, reformulated and
withdrawn; none of that may retrospectively change what a doctor actually
wrote.

**Specific decisions.**

- *`suggested_quantity()` returns `None` for PRN and irregular frequencies*
  rather than guessing. A wrong number on a prescription is worse than no
  number, because the pharmacist would dispense it.
- *A course needs a duration or an end date* unless it is PRN. This is how
  patients end up on antibiotics for months.
- *PRN requires an indication.* "Paracetamol as required" without saying what
  for is not a usable instruction.
- *`allow_substitution` is the prescriber's decision*, because some drugs are
  brand-specific for real clinical reasons.

**Affects.** `apps/prescriptions/`.

---

## 052 — Diagnoses are promoted to conditions deliberately
2026-09-02 · Clinical · decision

**What.** `promote_to_condition()` is an explicit action, not automatic.

**Why.** Most diagnoses are episodic. A chest infection should not follow
someone for life. A condition list that fills with every past complaint stops
being read — and a condition list nobody reads is worse than none, because the
system looks like it is carrying the information.

**Affects.** `apps/encounters/services.py`.

---

## 053 — Prescribing safety warns; it does not block
2026-09-02 · Prescribing · decision

**What.** `apps/prescriptions/safety.py` checks allergies (with cross-
sensitivity families), interactions and duplicates. High and critical warnings
require an **override reason**, captured and stored permanently. Nothing is
refused.

**Why.** A clinician may have a good reason to prescribe against a recorded
allergy — a documented mild rash decades ago, no alternative available, a
resuscitation. Software that refuses outright gets bypassed by writing the
prescription on paper, which loses the record entirely. Warning, capturing the
override and keeping both is the outcome that leaves evidence.

**Fails loud, not silent.** An *unconfirmed* allergy still warns. The cost of
a spurious warning is a click; the cost of a missed one can be anaphylaxis.

**The interaction list is deliberately small.** Eight high-severity pairs, not
a drug database. Shipping a partial list dressed up as comprehensive would be
worse than shipping none, because clinicians would trust the silence. A
licensed interaction database goes behind `check_interactions()` when the
pharmacy catalogue lands.

**Affects.** `apps/prescriptions/safety.py`, `apps/prescriptions/services.py`.

---

## 054 — Merged patient records were invisible to the safety checks
2026-09-02 · Prescribing · fix · **important**

**What.** `check_allergies()` and `run_safety_checks()` now call
`patient.resolve()` first. `create_prescription()` refuses outright to write
against a merged record and names the survivor.

**Why.** Found by the consultation seed. A patient with a recorded **severe
penicillin allergy** came back with **zero warnings**, and a prescription for
amoxicillin was accepted with no override.

The cause: merging moves allergies, conditions and identifiers to the
surviving record (entry 040) and leaves the merged row as a tombstone. Any
code path holding a reference to the retired record therefore sees a patient
with no allergies — and a safety check that finds nothing reports *clean*.

Silence is the most dangerous possible answer from a safety check. It is
indistinguishable from "this patient has no allergies".

`Patient.resolve()` had existed since entry 040 and nothing was calling it.

**How.** Defence in depth: resolve inside the checks so a stale reference
still gets the right answer, *and* refuse to prescribe against a tombstone at
all, since such a prescription would also be invisible on the patient's real
chart.

**Lesson.** Every read of clinical history must resolve the merge chain. Added
to the checklist's cross-cutting invariants.

**Affects.** `apps/prescriptions/safety.py`, `apps/prescriptions/services.py`.

---

## 055 — The cross-family allergy rule never fired
2026-09-02 · Prescribing · fix

**What.** `CROSS_FAMILY_RISK` keys are now normalised with `tuple(sorted(...))`
at import.

**Why.** The dictionary was keyed `("penicillin", "cephalosporin")`, but the
lookup builds `tuple(sorted(pair))`, which is `("cephalosporin",
"penicillin")` — `c` sorts before `p`. The key never matched, so prescribing a
cephalosporin to a penicillin-allergic patient produced **no warning at all**.

Confirmed by API test before and after: cefixime for a penicillin-allergic
patient went from 0 warnings to a moderate cross-reactivity warning.

**Why normalise rather than fix the literal.** A safety rule that depends on
whoever adds the next pair remembering alphabetical order is a rule waiting to
fail silently. Sorting at import makes the data impossible to write wrongly.

**Severity note.** Reported as *moderate*, not critical, and it does not force
an override — modern estimates put penicillin/cephalosporin cross-reactivity
around 2%, not the 10% once taught. A warning that cries wolf gets clicked
through, which costs more than it saves.

**Affects.** `apps/prescriptions/safety.py`.

---

## 056 — Vitals comparisons coerce before comparing
2026-09-02 · Clinical · fix

**What.** `VitalSigns.abnormal_flags()` and `.bmi` route every reading through
a `_numeric()` helper.

**Why.** `abnormal_flags()` raised `TypeError: '>=' not supported between
instances of 'str' and 'int'` when called on an instance that had not been
round-tripped through the database — a `DecimalField` still holds whatever the
caller assigned, often a string.

That matters more than an ordinary type error: a `TypeError` here means a
**fever goes unflagged**. A safety check that works only on the happy path is
not a safety check.

**Affects.** `apps/encounters/models.py`.

---

## 057 — Consultation screen
2026-09-02 · Frontend · feature

**What.** `Consultation.tsx` — vitals, SOAP note and prescribing on one page,
reached from the queue.

**Specific decisions.**

- *Laid out in the order a consultation happens*, top to bottom, so a
  clinician with ninety seconds never hunts for the next step.
- *Allergies and alerts pin above everything.* They are the one thing that
  must not be scrolled past.
- *Safety checks re-run as the prescriber types* (debounced 400 ms), so an
  allergy surfaces while they can still change their mind rather than as a
  rejection after they commit.
- *The override box only appears when a warning demands it*, and its
  placeholder says the reason is kept permanently — because it is.
- *Warnings sort most-severe first*, so a critical interaction is never below
  a duplicate-medicine notice.
- *Opening a consultation from the queue creates the encounter first*, then
  navigates. The server returns the existing open encounter if there is one,
  so a double click lands on the same chart rather than splitting a visit.

**Affects.** `frontend/src/pages/Consultation.tsx`,
`frontend/src/pages/Queue.tsx`, `frontend/src/types/index.ts`,
`apps/scheduling/serializers.py`.

---

## 058 — Billing: charges and invoices are separate things
2026-09-02 · Billing · feature

**What.** `apps.billing`: `ServiceItem`, `PriceList`, `PriceListItem`,
`Charge`, `Invoice`, `InvoiceLine`, `Payment`, `NumberSequence`.

**Why the split.** A charge records that something billable happened; an
invoice collects charges and asks for money. For an outpatient the two happen
minutes apart, so the distinction looks like overhead — until an inpatient
accumulates three weeks of charges before anyone bills them. Building the
invoice as the *only* record of a charge would make that impossible, and
retrofitting the split later would mean rewriting everything that touches
money.

**Prices are captured onto the charge.** The price list may change tomorrow;
what the patient was quoted today is what they are billed. The charge also
stores the service's name and code as text, so an invoice raised last year
still reads correctly after a service is renamed or retired.

**Affects.** `apps/billing/`.

---

## 059 — Every amount is a Decimal
2026-09-02 · Billing · decision

**What.** `DecimalField` throughout, `Decimal` arithmetic in the service
layer, and a `money()` helper that quantises to paisa with `ROUND_HALF_UP`.

**Why.** `0.1 + 0.2` is not `0.3` in binary floating point. A drift of one
paisa per line is invisible on one invoice and a reconciliation failure at the
end of a month.

**Why half-up rather than Python's default.** Banker's rounding is
statistically fairer but indefensible at a counter: a patient told that their
total rounds in their favour half the time and against them the other half
will not find the explanation satisfying, and neither will an auditor.

**Affects.** `apps/billing/models.py`, `apps/billing/services.py`.

---

## 060 — Invoice numbers come from a sequence table
2026-09-02 · Billing · decision

**What.** `NumberSequence`, one row per facility per fiscal year per document
type, incremented under `select_for_update`.

**Why not `MAX(number) + 1`.** It is not concurrency-safe — two counters
issuing at the same moment take the same number — and it is wrong the moment a
row is deleted. Nepal requires sequential, gapless invoice numbering per
fiscal year, and an audit that finds a missing number will ask why.

**The number is allocated at issue, not at creation.** An abandoned draft
would otherwise consume a number and leave exactly the gap the requirement
exists to prevent.

**Affects.** `apps/billing/models.py`, `apps/billing/services.py`.

---

## 061 — Issued invoices are reversed, never edited
2026-09-02 · Billing · decision

**What.** A credit note is a second `Invoice` row with `is_credit_note=True`
and negative quantities, sharing the numbering sequence.

**Why the same table.** A credit note is an invoice with negative intent. It
needs a number from the same statutory sequence, and every report that lists
invoices must list it. A separate model would mean duplicating the line
structure, the numbering and every query.

**Why negative quantities rather than negative prices.** The unit price is
what was charged, and a report grouping by service should still show the right
rate against a reversed quantity.

**Affects.** `apps/billing/models.py`, `apps/billing/services.py`.

---

## 062 — Nepali fiscal year, and an approximation stated openly
2026-09-02 · Billing · decision

**What.** `apps/billing/fiscal.py` pins the fiscal year boundary at 16 July
and derives the Bikram Sambat label by offset — producing "2083/84".

**Why an approximation.** Shrawan 1 drifts between 16 and 17 July, because BS
is lunisolar and its month lengths are published annually rather than
computed. Bundling a conversion table is a real dependency with its own
maintenance burden.

**Why it is written down rather than hidden.** The error is at most one day
per year, affecting only documents issued on that day — but this drives
statutory invoice numbering. The module docstring says plainly that it must be
replaced with a proper BS calendar before any statutory VAT filing, and names
`fiscal_year_for()` as the single seam to swap. An approximation someone knows
about is a manageable risk; one they discover during an audit is not.

**Affects.** `apps/billing/fiscal.py`.

---

## 063 — Price resolution is layered, and reports its source
2026-09-02 · Billing · feature

**What.** `resolve_price()` walks category-and-facility → category →
facility → the service's own default, breaking ties on `priority`, and returns
the chosen source alongside the number.

**Why.** The same consultation costs a general patient NPR 800 and a Nepal
Telecom employee NPR 650. Modelled as separate price lists rather than a
blanket discount, because negotiated rates differ per service — a scheme may
get 20% off consultations and nothing off laboratory work.

The source is returned for the same reason entitlements carry provenance:
"why was I charged this?" is a question a clerk has to answer with a patient
standing in front of them.

**Affects.** `apps/billing/services.py`.

---

## 064 — Refunds were double-counted in every total
2026-09-02 · Billing · fix · **important**

**What.** Every query that sums payments now includes both `COMPLETED` and
`REFUNDED` statuses, via a named `BANKED_STATUSES` constant.

**Why.** Found by reading the seeded cash-up: **cash showed −500 when 1000 was
genuinely in the drawer**, and a credited patient showed as having paid
−1500 rather than nothing.

A refund is recorded two ways at once, and only one of them may be counted:

* the original payment is marked `REFUNDED`, **and**
* a second payment row is created carrying the negative amount.

Summing only `COMPLETED` excluded the original while keeping its reversal, so
the refund was subtracted a second time. On one invoice the error is obvious;
across a day's takings it is a till that does not balance and nobody knows
why.

**How.** Include both. A refunded payment *did* happen — the money arrived and
was banked — and its reversal is the separate negative row. Together they net
to zero, which is the truth.

The alternative (drop the negative row, rely on the status alone) was
rejected: two rows is the honest record of two real movements of cash, and a
till reconciliation needs to see both.

**Also fixed alongside.** A credited invoice was flipping back to `issued`
when the refund landed, making a settled document look like an open demand for
money that no longer existed. `CREDITED` is now terminal.

**Lesson.** When a reversal is recorded both as a status change *and* as a
compensating row, exactly one of them may feed a total. Worth checking
wherever this pattern recurs — stock adjustments and payroll corrections will
both have it.

**Affects.** `apps/billing/services.py`.

---

## 065 — Outstanding is floored at zero
2026-09-02 · Billing · decision

**What.** `patient_account()` reports `outstanding` as never negative, and
reports money owed *to* a patient separately as `credit_balance`.

**Why.** A negative amount owed is not something a patient or a clerk can act
on, and "outstanding: −1500" reads as a bug even when the arithmetic is right.
They are two different concepts and deserve two fields.

**Affects.** `apps/billing/services.py`.

---

## 066 — Billing counter screen
2026-09-02 · Frontend · feature

**What.** `Billing.tsx` — patient search, charge capture, invoice, payment and
account history on one screen.

**Specific decisions.**

- *One screen, running total always visible.* The first question every patient
  asks is "how much?", and the clerk should never navigate to answer it.
- *The payment amount pre-fills with the full balance.* Settling in full is
  the common case, and retyping a number already on screen invites a typo.
- *The reference field only appears for non-cash methods.* A cash payment has
  no transaction id, and an empty box invites someone to put something in it.
- *Wallets are named individually* — eSewa, Khalti, Fonepay — because
  reconciliation is per provider; each settles separately and on its own
  schedule.
- *Amounts render with no decimals.* Nepali counters rarely deal in paisa, and
  trailing `.00` on every line is noise.

**Affects.** `frontend/src/pages/Billing.tsx`,
`frontend/src/types/index.ts`, `frontend/src/App.tsx`.
