# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# TypeScript incremental build cache. Machine-specific, rewritten by every
# build, and committed once by accident -- it turned up as a modified file in
# the diff of a change that touched no TypeScript configuration at all.
tsconfig.tsbuildinfo
*.tsbuildinfo
